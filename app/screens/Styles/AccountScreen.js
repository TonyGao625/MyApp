import { StyleSheet } from 'react-native'

export default Styles = StyleSheet.create({
    view: {
        flex: 1,
    },
    fullView: {
        flex: 1,
    },
    imageStyle: {
        alignItems: 'center',
        justifyContent: 'center'
    }
})